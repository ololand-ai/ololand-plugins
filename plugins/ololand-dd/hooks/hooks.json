{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"${CLAUDE_PLUGIN_ROOT}\" ] && bash \"${CLAUDE_PLUGIN_ROOT}/scripts/setup_gate.sh\" || echo \"[ololand-dd] CLAUDE_PLUGIN_ROOT unset; setup gate skipped\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
